# Generate docker-compose.yaml Workflow Configuration
name: generate-docker-compose
description: "Gera arquivo docker-compose.yaml conforme com a plataforma Embrapa I/O baseado em template adaptável"
author: "BMad Embrapa I/O Module"
version: "1.0.0"

# Critical variables from config_source
config_source: "{project-root}/bmad/embrapa-io/config.yaml"
output_folder: "{config_source}:output_folder"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
date: system-generated

# Workflow paths
installed_path: "{project-root}/bmad/embrapa-io/workflows/setup/generate-docker-compose"
template: "{installed_path}/template.docker-compose.yaml"
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"

# Project configuration
project_name: ""

# Output configuration
default_output_file: "{project-root}/docker-compose.yaml"

# Web bundle configuration
web_bundle:
  name: "generate-docker-compose"
  description: "Gera arquivo docker-compose.yaml conforme com a plataforma Embrapa I/O"
  author: "BMad Embrapa I/O Module"
  version: "1.0.0"

  web_bundle_files:
    - "bmad/embrapa-io/workflows/setup/generate-docker-compose/workflow.yaml"
    - "bmad/embrapa-io/workflows/setup/generate-docker-compose/instructions.md"
    - "bmad/embrapa-io/workflows/setup/generate-docker-compose/template.docker-compose.yaml"
    - "bmad/embrapa-io/workflows/setup/generate-docker-compose/checklist.md"
    - "bmad/embrapa-io/templates/docker-compose/base.yaml"
    - "bmad/embrapa-io/knowledge/embrapa-io-fundamentals.md"
    - "bmad/embrapa-io/knowledge/embrapa-io-validation.md"
